# План работ

Приоритеты взяты из [аудита](AUDIT.md).

## Этап 1. Сделать бота рабочим (блокеры)

- [ ] Реализовать `anims.start_session(bot, cfg, msg, chat_id, sessions)` — P0-1.
- [ ] Перевести `python` на venv и добавить миграцию `config_version: 6` — P0-2.
- [ ] Закрыть соединения SQLite через `contextlib.closing` — P0-3.
- [ ] Тест: вызов `handlers.on_media` с фейковым `Message`.
- [ ] Тест: число открытых дескрипторов не растёт после 50 записей в журнал.

## Этап 2. Стабильность

- [ ] Отдельный таймаут для загрузки G-code — P1-5.
- [ ] Лимит стартов сервиса (`StartLimitIntervalSec`, `StartLimitBurst`) — P1-8.
- [ ] `set -E` и корректный откат в установщике — P1-2, P1-3, P1-4.
- [ ] Резать `/error <id>` под лимит Telegram или отправлять файлом — P1-6.
- [ ] Уровни уведомлений: аварийные игнорируют тихие часы — P1-7.

## Этап 3. Гигиена кода

- [ ] Единая версия во всех файлах (`__init__.py` — единый источник).
- [ ] TTL и лимит для `PENDING`, `SESSIONS`, `RENAMING`, `MOVE_BY_CHAT`, `LOCKS`.
- [ ] Ретенция `errors.db` (по возрасту и размеру).
- [ ] `read_klippy_tail` без чтения всего файла в память.
- [ ] Экспоненциальный backoff для WebSocket.
- [ ] Согласовать таймауты `convert_params` и `animation_ffmpeg_timeout`.
- [ ] Исправить `u:noop` / `m:noop` и `ui.cycle`.
- [ ] Сверка `pending["index"]` и `pending["digest"]` для макросов; запись `PENDING` для `p:do:`.
- [ ] Точная проверка опасных макросов (по токенам, не по подстроке).

## Этап 4. Безопасность

- [ ] Отдельный системный пользователь вместо `root`.
- [ ] Поддержка API-ключа Moonraker и HTTPS.
- [ ] Проверка синтаксиса Klipper перед записью в `printer.cfg`.
- [ ] Загрузка токена из файла или systemd-credentials.
- [ ] Роли: наблюдатель и оператор.

## Этап 5. Функции

- [ ] Приём видео напрямую (mp4/webm/mov), а не только документом `.gif`.
- [ ] Превью анимации в чат до применения (`preview_base`, `clean_preview`).
- [ ] Очистка старых `backups/upgrade_*`.
- [ ] Автоматический выбор анимации по событию печати (старт, пауза, финиш).
- [ ] Перенос `tests/test_converter.py` на путь из переменной окружения — T-3.

## Этап 6. Инфраструктура

- [ ] Перенести `ci/ci.yml` в `.github/workflows/ci.yml`.
- [ ] Добавить линтеры: `ruff`, `shellcheck`, `bandit`.
- [ ] Покрытие тестами для `handlers.py` и `ws.py`.
- [ ] Выбрать лицензию и добавить `LICENSE`.
