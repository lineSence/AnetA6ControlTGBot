# План работ

Приоритеты взяты из [аудита](AUDIT.md). Что уже исправлено — в [FIXES.md](FIXES.md).

## Сделано в 2.6.0

### Блокеры

- [x] Реализована `anims.start_session(bot, cfg, msg, chat_id, sessions)` — P0-1.
- [x] `python` переведён на venv, добавлена миграция `config_version: 6` — P0-2.
- [x] Соединения SQLite закрываются через `contextlib.closing` — P0-3.
- [x] Тест: вызов `handlers.on_media` с фейковым `Message`.
- [x] Тест: число открытых дескрипторов не растёт после серии записей в журнал.

### Стабильность

- [x] Отдельный таймаут `upload_timeout` для загрузки G-code — P1-5.
- [x] Лимит стартов сервиса: `StartLimitIntervalSec=300`, `StartLimitBurst=5` — P1-8.
- [x] `set -Eeuo pipefail`, перехват `EXIT` и полный откат в установщике — P1-2, P1-3, P1-4.
- [x] `/error <id>` режется под лимит Telegram — P1-6.
- [x] Аварийные уведомления игнорируют тихие часы — P1-7.
- [x] `ReadWritePaths=-/var/log/tg2printer.log` в юните — P1-1.

### Гигиена кода

- [x] Единая версия во всех файлах, источник — `tgbot/__init__.py`.
- [x] TTL и фоновая уборка для `PENDING`, `SESSIONS`, `RENAMING`, `MOVE_BY_CHAT`, `LOCKS`.
- [x] Ретенция `errors.db` через `error_log_keep`.
- [x] `read_klippy_tail` читает хвост через `deque`, а не весь файл.
- [x] Экспоненциальный backoff для WebSocket: от 3 до 60 секунд.
- [x] Таймаут `convert_params` считается от `animation_ffmpeg_timeout`.
- [x] Все пустые кнопки шлют обрабатываемый `m:noop`; `ui.cycle` не падает на чужом значении.
- [x] Сверка `index` и `digest` для макросов, запись `PENDING` для `p:do:`.
- [x] Опасные макросы ищутся по границам слова, а не по подстроке.

### Безопасность

- [x] Поддержка API-ключа Moonraker (`moonraker_api_key`, заголовок `X-Api-Key`).
- [x] Проверка синтаксиса `printer.cfg` до и после правки, откат при поломке.
- [x] Токен можно передать через переменную `TG_BOT_TOKEN`, конфиг пишется с правами `600`.

### Функции

- [x] Приём анимаций, видео, фото и документов с проверкой формата и лимита 20 МБ.
- [x] Предпросмотр анимации до применения: свой временный каталог, чистится после отправки.
- [x] Чистка старых `backups/upgrade_*` по `UPGRADE_KEEP`.

## Осталось: безопасность

- [ ] Отдельный системный пользователь вместо `root`.
- [ ] HTTPS и TLS до Moonraker (нужен реверс-прокси и сертификаты).
- [ ] Загрузка токена из systemd-credentials.
- [ ] Роли: наблюдатель и оператор.

## Осталось: функции

- [ ] Отправка превью как video-сообщения, а не GIF или PNG.
- [ ] Автоматический выбор анимации по событию печати (старт, пауза, финиш).
- [ ] Перенос `tests/test_converter.py` на путь из переменной окружения — T-3.

## Осталось: инфраструктура

- [ ] Перенести `ci/ci.yml` в `.github/workflows/ci.yml` (нужен токен с правом на workflow).
- [ ] Добавить линтеры: `ruff`, `shellcheck`, `bandit`.
- [ ] Поднять покрытие `handlers.py` и `ws.py` (сейчас в наборе 35 тестов).
- [ ] Выбрать лицензию и добавить `LICENSE`.

## Осталось: проверка на железе

- [ ] Прогон установщика на Anet A6 с Klipper: сначала `NO_SERVICE=1`, затем полный запуск.
- [ ] Проверка отката: сломать установку на шаге `pip` и убедиться, что прежняя версия вернулась.
- [ ] Проверка анимаций на живом дисплее принтера.
